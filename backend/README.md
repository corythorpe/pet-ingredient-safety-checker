# Backend

The main application runs from the project root: **`app.py`** (Flask with DigitalOcean GenAI agents, file cache, and `ingredient_database.json`).

This directory is reserved for future backend modules or an alternative API if needed.
